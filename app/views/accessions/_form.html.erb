<%= form_for(@accession) do |f| %>
  <% if @accession.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@accession.errors.count, "error") %> prohibited this accession from being saved:</h2>

      <ul>
      <% @accession.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :accession_number %><br>
    <%= f.text_field :accession_number %>
  </div>
  <div class="field">
    <%= f.label :collection_date %><br>
    <%= f.date_select :collection_date %>
  </div>
  <div class="field">
    <%= f.label :date_received %><br>
    <%= f.date_select :date_received %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
